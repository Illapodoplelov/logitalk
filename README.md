# Це графічна програма чату, зроблена на Python із використанням бібліотеки CustomTkinter.
Вона дозволяє користувачам спілкуватися через сервер, надсилати повідомлення та зображення.

⚙️ Основні можливості:

Підключення до сервера
Програма намагається з’єднатися з локальним сервером (localhost:8080).
Якщо з’єднання успішне — статус стає “🟢 Онлайн”.
Якщо ні — показується повідомлення про помилку.

Обмін повідомленнями

Можна вводити текст і надсилати його кнопкою або клавішею Enter.

Програма підтримує емодзі — наприклад, :) → 😊, <3 → ❤️.

Усі повідомлення показуються у великому вікні чату.

Надсилання зображень

Можна вибрати картинку з комп’ютера (через кнопку 📂).

Вона відправляється на сервер у форматі base64, а також показується локально в чаті.

Меню з налаштуваннями

Меню можна відкривати/закривати кнопкою ▶ або ◀.

У меню можна:

змінити своє ім’я (нікнейм);

зберегти історію чату у файл chat_history.txt;

очистити чат;

перемкнути тему (світла/темна).

Збереження та очищення

Історія чату зберігається у звичайний текстовий файл.

Кнопка 🧹 очищує всі повідомлення на екрані.

Інтерфейс

Програма адаптується під розмір вікна.

Використовуються сучасні елементи (CustomTkinter) для приємного вигляду.
